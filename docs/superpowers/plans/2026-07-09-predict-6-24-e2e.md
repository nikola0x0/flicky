# Predict 6-24 Migration — Plan 4: E2E + deploy gates

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Prove the migrated 6-24 stack works end-to-end on live testnet — a real `Duel<DUSDC>` create → join → reveal → **atomic sponsored-shaped swipe that mints a real 6-24 position** (`mint_exact_quantity` + `duel::record_swipe`) → settle (keeper-fed) → finalize → payout — and close the carried deploy gates.

**Architecture:** Rewrite `apps/server/src/scripts/e2e.test.ts` (currently 100% 4-16: `OracleSVI`/`PredictManager`/`predict::create_manager`/JSON-RPC `queryEvents`) into a standalone 6-24 flow. It drives the chain directly with `Ed25519Keypair`s paying their own gas (a test harness — it does NOT need the server, DB, or the Enoki sponsor running; it exercises the SAME PTB shapes the web builds). Two funded accounts play a PvP duel. Settlement uses the dev shortcut `finalize_test_one_price` to avoid waiting for real oracle settlement (a separate optional slow path validates real `settle_card` + `redeem_settled`).

**Tech Stack:** `bun test`, `@mysten/sui` (`Transaction`, `Ed25519Keypair`, `coinWithBalance`, gRPC client), live testnet DeepBook Predict 6-24 + published flicky `0x6c6be720…`.

## Global Constraints
- Use `bun` only. Run via `bun test:e2e` (`bun test src/scripts/e2e.test.ts`) from `apps/server`. This is OPT-IN (not in the default `bun run test` suite).
- **All 6-24 IDs, PTB recipes, tick units in `docs/superpowers/specs/2026-07-09-predict-6-24-offchain-reference.md` — READ FIRST.** `AccumulatorRoot = 0xacc`, `Clock = 0x6`. Discovery: predict indexer `https://predict-server-beta.testnet.mystenlabs.com/markets`.
- **Verified live (2026-07-10):** all 6-24 shared objects resolve — `ProtocolConfig 0x2325…` (`::protocol_config::ProtocolConfig`), `AccountRegistry 0x3c54…`, `OracleRegistry 0xf3de…`, `0xacc` (`0x2::accumulator::AccumulatorRoot`); short markets live (1m/5m expiries in the future); the test deployer key `0x9826b0…` holds **92.62 dUSDC + 3.48 SUI**.
- The 6-24 flicky ABI (published): `create_duel<T>(stake, deck_hash, deck_size, ctx)`, `join_duel<T>(duel, stake, clock, ctx)`, `reveal_deck<T>(duel, cards)`, `new_card(expiry_market_id: ID, strike: u64)`, `record_swipe<T>(duel, cardIdx, isUp, quantity, orderId: u256, clock, ctx)`, `finalize_test_one_price<T>(duel, price, clock, ctx)`, `settle_card<T>(duel, p0Wrapper, p1Wrapper, cardIdx, settlementPrice, p0Premium, p1Premium)`, `finalize<T>`. **The duel stake coin is dUSDC** (`Duel<DUSDC>`) — `record_swipe`/`settle_card`/`finalize` type arg = the dUSDC type.
- 6-24 mint: `expiry_market::mint_exact_quantity(market, wrapper, auth, config, pricer, lower_tick, higher_tick, quantity, leverage=1e9, max_cost=u64::max, max_probability=u64::max, 0xacc, 0x6, ctx)` → u256 order_id, chained into `record_swipe`. Pricer from `load_live_pricer(market, config, oracleRegistry, pyth, bsSpot, bsForward, bsSvi, 0x6)`. Account: `account_registry::new` → `account::share` → `account::generate_auth` → `account::deposit_funds<DUSDC>(wrapper, auth, coin, 0xacc, 0x6)`.
- Branch `feat/predict-6-24-migration` (continues from Plan 3, HEAD `ade3b68`). Whole monorepo typechecks clean.

**Scope note:** This plan RUNS live testnet transactions (spends the test key's SUI gas + dUSDC). It is a validation harness — real on-chain aborts (feed staleness, tick admission, mint pause, insufficient cash) are expected debugging surface, not plan failures. Iterate the run task until green or a genuine upstream blocker is identified and documented.

## File structure
- `apps/server/src/scripts/e2e.test.ts` — rewritten 6-24 E2E (delete all 4-16 helpers).
- `apps/server/src/scripts/check-6-24-live.ts` (new) — a standalone gate-verification script (`bun run` it) that asserts the deploy gates.

---

## Task 1: Deploy-gate verification script

**Files:** Create `apps/server/src/scripts/check-6-24-live.ts`; add a `check:6-24` script to `apps/server/package.json`.

**Interfaces:** Produces a runnable check that exits 0 when all gates pass, printing each.

- [ ] **Step 1: Write the gate checks**
A script that, using the gRPC client (`getSuiClient`) + `fetch`, verifies and prints:
  1. Each 6-24 shared object resolves and has the expected `objType`: `ProtocolConfig`, `AccountRegistry` (account pkg), `OracleRegistry` (propbook pkg), `PoolVault`, `0xacc` = `0x2::accumulator::AccumulatorRoot`. (ids from the reference doc / `env.ts`.)
  2. `GET {predictIndexerUrl}/markets` returns ≥1 BTC (`propbook_underlying_id===1`) market with `expiry > now` (live short market).
  3. **`cashflow.net_premium` field gate:** `GET {predictIndexerUrl}/markets/{liveMarketId}/positions/{someOrderId}/cashflow` — probe the endpoint shape and confirm the `net_premium` field name (the keeper's `readOrderPremium` depends on it). Use a real order id if the E2E has minted one (Task 3 can call back in); otherwise probe with a known/any id and record the response shape + whether `net_premium` is the field. If no populated row exists yet, print the endpoint's empty/`null` response shape so the field name is at least confirmed structurally.
  4. Feed freshness: `load_live_pricer` devInspect against a live market succeeds (feeds not stale) — or note the exact abort code if it fails.

- [ ] **Step 2: Run it**
`cd apps/server && bun run check:6-24`. Expected: all gates print ✅ (or a clear ❌ with the failing gate + reason). Record output.

- [ ] **Step 3: Commit**
```bash
git add apps/server/src/scripts/check-6-24-live.ts apps/server/package.json
git commit -m "test(e2e): 6-24 deploy-gate verification script"
```

---

## Task 2: Rewrite e2e.test.ts — 6-24 flow (build, not yet run live)

**Files:** Rewrite `apps/server/src/scripts/e2e.test.ts`

Delete every 4-16 helper (`findSettledBtcOracle`, `OracleSVI` interface, `PredictManager` resolution, `predict::create_manager`, JSON-RPC `queryEvents`). Build the 6-24 flow with clear helpers.

**Interfaces:** Produces a `bun test`-runnable suite. Helpers: `fundedPlayer(name)` (derive/fund a keypair), `setupAccount(kp)` (create + share wrapper, deposit dUSDC), `discoverMarkets(n)` (predict indexer `/markets`, live BTC, → `{expiryMarketId, strike, tickSize}` cards), `atomicSwipe(kp, duelId, wrapperId, card, cardIdx, isUp, qty)` (the mint+record_swipe PTB), `settleAndFinalize(duelId, price)`.

- [ ] **Step 1: Two funded players**
Player 0 = the deployer key (`SUI_KEEPER_PRIVATE_KEY`/`SUI_DEPLOYER_PRIVATE_KEY` from env — has SUI + dUSDC). Player 1 = a fresh `Ed25519Keypair.generate()`; fund it from Player 0 with a `Transaction` that transfers ~1 SUI gas + a `coinWithBalance` dUSDC split (enough to stake + deposit). Wait for the funding tx. (Both players need dUSDC for the duel stake + account premium.)

- [ ] **Step 2: Account setup helper**
`setupAccount(kp)`: if `account_registry::derived_wrapper_exists(registry, addr)` is false, sign a PTB `account_registry::new(registry)` → `account::share(wrapper)`; then a deposit PTB `account::generate_auth` + `deposit_funds<DUSDC>(wrapper, auth, coinWithBalance(dusdc), 0xacc, 0x6)`. Resolve the wrapper id via `derived_wrapper_address(registry, addr)` (devInspect) or the `AccountCreated` event. Return the wrapper id.

- [ ] **Step 3: Discover markets → cards**
`discoverMarkets(n)`: `GET /markets`, filter `propbook_underlying_id===1 && expiry>now+headroom`, take n; for each, read `tick_size`; derive a `strike` near spot (read spot from propbook `/oracles/{pyth}/pyth/latest` or use the market reference tick). Return cards `{expiryMarketId, strike, tickSize}`. Compute `deck_hash = sha256(bcs(cards mapped to Move Card{expiry_market_id, strike}))` matching the on-chain reveal check.

- [ ] **Step 4: Create → join → reveal**
Player 0: `create_duel<DUSDC>(coinWithBalance(dusdc stake), deckHash, deckSize, ctx)` → capture the `Duel<DUSDC>` shared object id from objectChanges. Player 1: `join_duel<DUSDC>(duel, coinWithBalance(dusdc stake), 0x6, ctx)`. Player 0: `reveal_deck<DUSDC>(duel, [new_card(expiryMarketId, strike)...])`.

- [ ] **Step 5: Atomic swipe helper**
`atomicSwipe(kp, duelId, wrapperId, card, cardIdx, isUp, qty)`: ONE PTB — `account::generate_auth` → `load_live_pricer(market, config, oracleRegistry, pyth, bsSpot, bsForward, bsSvi, 0x6)` → `mint_exact_quantity(market, wrapper, auth, config, pricer, lowerTick, higherTick, qty, 1e9, u64max, u64max, 0xacc, 0x6)` → `record_swipe<DUSDC>(duel, cardIdx, isUp, qty, <mint result>, 0x6)`. `lowerTick/higherTick` from `deriveTicks(strike, isUp, tickSize)` (UP=(strike/tick, posInf], DOWN=[0, strike/tick]). Sign with `kp`. Both players swipe all cards in order.

- [ ] **Step 6: Settle + finalize + assert**
Use the dev shortcut: `finalize_test_one_price<DUSDC>(duel, settlementPrice, 0x6, ctx)` (one fed price settles all cards free-style + finalizes) — signed by Player 0. Read the `DuelFinalized` event; assert a winner (or tie) and that the pot was distributed. (Add a SKIPPED/optional test scaffold for the REAL path: `settle_card<DUSDC>(duel, p0Wrapper, p1Wrapper, i, settlementPrice, p0Premium, p1Premium)` × N + `finalize` + `redeem_settled`, gated on a market actually settling — document it as the slow path.)

- [ ] **Step 7: typecheck + commit (do NOT run live yet)**
`cd apps/server && bunx tsc --noEmit 2>&1 | grep -c "error TS"` → 0 (or ≤ the current baseline; e2e.test.ts must not add new errors). Commit `test(e2e): rewrite e2e for 6-24 flow (build)`.

---

## Task 3: Run the E2E live + iterate to green

**Files:** `apps/server/src/scripts/e2e.test.ts` (fixes from real failures), report.

- [ ] **Step 1: Ensure the env key is set**
Confirm `SUI_DEPLOYER_PRIVATE_KEY` (or the key the test reads) is in `apps/server/.env.local` and the address holds SUI + dUSDC (`sui client balance`). If Player 1 funding fails for lack of dUSDC, reduce stake/deposit amounts.

- [ ] **Step 2: Run**
`cd apps/server && bun test:e2e 2>&1 | tail -60`. Capture the result.

- [ ] **Step 3: Debug real on-chain aborts (iterate)**
Expected failure modes + fixes:
  - `ELivePricingExpired`/feed-stale on `load_live_pricer` → pick a fresher/further-out market; confirm feed ids are the current registry-bound ones.
  - Tick admission abort → snap `lower_tick`/`higher_tick` to `admission_tick_size`.
  - Insufficient dUSDC in account → deposit more before minting; ensure premium + fees are covered.
  - `mint_paused`/`trading_paused` → pick another market / note the gate.
  - deck_hash mismatch on reveal → ensure the BCS serialization of `Card{expiry_market_id, strike}` matches the Move struct field order exactly.
  Fix each, re-run. Do NOT weaken assertions to force a pass — if a real upstream blocker (e.g. all markets paused) prevents completion, STOP and document it precisely.

- [ ] **Step 4: Close the premium-field gate**
Once a real position is minted (Step 2 success), call the Task 1 check against that market/order id and confirm `GET /markets/{id}/positions/{orderId}/cashflow` returns a populated `net_premium` — closing the carried Plan-2 deploy gate. Record the actual field/value.

- [ ] **Step 5: Commit + report**
Commit any fixes `test(e2e): 6-24 e2e green on testnet` (or, if blocked, `test(e2e): 6-24 e2e harness + documented upstream blocker`). Write `docs/superpowers/specs/2026-07-10-6-24-e2e-results.md` with: the full run output, which gates closed, tx digests of a successful mint + finalize, and any upstream blocker.

---

## Verification (whole plan)
- `bun run check:6-24` → all deploy gates ✅.
- `bun test:e2e` → the full create→join→reveal→**real 6-24 mint**→settle→finalize→payout passes on testnet (or a precise upstream blocker is documented with evidence).
- The `cashflow.net_premium` field gate is closed (real populated row observed), or documented as still-empty with the confirmed endpoint shape.
- A successful real `mint_exact_quantity` tx digest is recorded — proving the atomic swipe invariant works on 6-24.

## Migration close-out (after this plan)
The 4-plan migration (contract → server → web → E2E) is complete. Remaining optional cleanups (non-blocking, tracked): delete dead `App.tsx`; team-confirm nothing else assumes SUI stake; consider a repo-wide prettier-normalization PR (separate from this branch). Then finish the branch (merge/PR) via superpowers:finishing-a-development-branch.
