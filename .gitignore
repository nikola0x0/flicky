# Dependencies
node_modules
.pnp
.pnp.js

# Local env files
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# Testing
coverage

# Turbo
.turbo

# Vercel
.vercel

# Build Outputs
dist

# Debug
npm-debug.log*

# Misc
.DS_Store
*.pem

# Local scratch
local/
# Local reference clones (e.g. deepbookv3 source read for docs) — never tracked.
# Was accidentally committed as an orphan gitlink (no .gitmodules) which broke CI.
temp/

# Move build + codegen outputs
apps/contracts/build/
apps/contracts/package_summaries/

# Server-side runtime state (deckmaster plaintext store etc.)
apps/server/.data/

# Local Claude Code state
.claude/

# Agent scratch/session dirs that have leaked into commits before
# (e.g. apps/brain/<uuid>/scratch/...) — not a real workspace, never track it.
apps/brain/

# superpowers brainstorm artifacts (local)
.superpowers/

# Local Python venv (rembg background-removal tool)
.venv-rembg/
